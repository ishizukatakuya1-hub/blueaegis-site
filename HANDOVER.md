# 引継ぎ書 — Blue Aegis コーポレートサイト

作成: 2026年8月22日 / 前セッション（Claude Code）から新規セッションへ

未着手の依頼は **§8 SEO自動化** です。それ以外は現状の把握と、踏んではいけない地雷の共有が目的です。

---

## 1. これは何か

Blue Aegis株式会社（知的財産のライセンス事業＋規制対応技術の研究開発＋不動産管理）のコーポレートサイト。GitHub Pages上の静的サイトで、GitHub Actionsでビルドして配信している。

| 項目 | 値 |
|---|---|
| 公開URL | http://blueaegis.co.jp/ |
| リポジトリ | https://github.com/ishizukatakuya1-hub/blueaegis-site |
| ローカル | `C:\Users\user\Desktop\Clodecodeapp\blueaegis-site` |
| デフォルトブランチ | `main` |
| ビルド | `node tools/build.js` → `_site/`（依存パッケージなし） |
| 配信 | `.github/workflows/pages.yml`（mainへのpushで自動） |
| DNS | Xserverドメイン管理。Aレコード4件がGitHub Pages向き |

---

## 2. サイト構成

```
/                                        日本語トップ
/en/                                     英語トップ
/insights/                               規制解説（手作業・4本）
  eu-ai-act-transparency.html            EU AI法の透明性義務
  aml-ekyc-2027.html                     犯収法2027年4月改正
  housing-safety-net-postmortem.html     住宅セーフティネット法と死後事務
  eudi-wallet-relying-party.html         EUデジタルIDウォレット依拠当事者登録
/en/insights/
  eu-ai-act-transparency.html            上記1本の英語版
/blog/                                   Blue Aegis Media（自動掲載・1本）
```

`style.css` と `script.js` は全ページ共通。英語版の差分は `html[lang="en"]` で上書きしている。
**CSSのフェードイン対象セレクタと `script.js` のセレクタは同一に保つこと。** 片方だけ変えると要素が非表示のまま残る。

---

## 3. 記事の自動掲載（Cowork連携）

別セッション（Cowork/クラウド）の定期タスクが毎朝、`content/blog/` にMarkdownをpushしてPRを作る。

- **契約書 = `PUBLISHING.md`**（リポジトリ直下）。生成側は毎回これを読む。**仕様を変えたらここを直す。**
- PRで `.github/workflows/validate.yml` が走り、frontmatter・出典・ファイル名・ビルドを検証する
- `draft: true` は公開ビルドから完全に除外される
- 出典は frontmatter の `sources` から記事末尾に自動出力される。本文に「参考・出典」を書くと二重になる

**規制解説（`/insights/`）と日次ブログ（`/blog/`）は系統が違う。** 読者層も更新頻度も異なるため分けてある。自動掲載の対象は `/blog/` のみ。

---

## 4. ★踏んではいけない地雷

### 4-1. 自社の将来出願に対する先行技術

**これが最重要。** この会社は特許出願を140件超保有し、多くが国内優先権・分割・PCTの選択肢を残したまま係属中。

**記事に「解決の仕組み」を書くと、自社公開が将来の追加出願に対する先行技術になる。** 国内優先権で新たな内容を追加する場合、追加部分は後の出願日で判断されるため。

**確立した方針：規制の内容と事業者が直面する課題までを書き、解決の仕組みは書かない。** 締めは「運用でしのぐか、仕組みで担保するか」という一般論に統一している。

実際に一度違反しており、EU AI法の記事から「確認できなかった状態を成功とも失敗とも区別して記録する」という記述を削除した（CP01・特願2026-082900の国優期限2027-05-15の選択肢を守るため）。

関係する出願と期限：

| 記事 | 対応する出願 | 守るべき期限 |
|---|---|---|
| EU AI法 | CP01 特願2026-082900 | 国優 2027-05-15 |
| 犯収法 | KY01 特願2026-187663 | 国優 2027-08-12 |
| 住宅SN法 | LV06 特願2026-187529 | 国優 2027-08-11 |
| EUDIウォレット | DI01 特願2026-188326 | PCT 2027-08-13（EP移行前提） |

### 4-2. 未出願案件の分野を書かない

未送信の出願が4件ある（物流実績報告・クロスメディア視聴者測定・リコール・廃棄検証）。**これらの分野は技術領域にも記事にも一切書いていない。** 引例を作られる、または先に出願される実害があるため。

### 4-3. 事実の裏取り

ブランドガイドラインが「証明できる範囲を超えたことは述べない」と定めており、記事の書き方もこれに合わせている。

- 法令の条番号・施行日は**一次資料で確認する**。社内メモにあっても裏を取る
- 実際に、社内メモにあった「EU AI法の執行主体にEDPSを含む」「制裁は全世界売上高3%」は欧州委員会の公式発表に記載がなかったため記事に書いていない
- 犯収法の記事では、ベンダー各社の解説に流布している「あらかじめ代替手順を定める必要がある」が一次資料に見当たらない旨を明記している。これが差別化点になっている

### 4-4. 認証情報

**トークン・パスワードは受け取らない・転送しない。** 過去にGitHub PATがスクリーンショットで会話に流入し、再発行を依頼した経緯がある。Cloudflareの解析トークン（公開ページに埋め込む前提の値）は例外。

### 4-5. 個人情報

会社概要から**代表者名・所在地・法人番号を意図的に削除している**（ユーザー判断）。復活させないこと。連絡先は `info@blueaegis.co.jp` のみ。

---

## 5. 検証のやり方

**プレビュー機能（`preview_start` の url指定）は使えない。** ページを `data:` URL として読むため、`style.css` などの相対参照が解決できない。

代わりにローカルHTTPサーバーを使う。`.claude/launch.json` に定義済み。

```
preview_start { name: "blueaegis-site" }   → http://localhost:4890（ソース）
preview_start { name: "blueaegis-build" }  → http://localhost:4891（_site/）
```

**必ず確認すること：**
- フェードイン対象要素が最終的にすべて表示されるか（`opacity >= 0.9`）
- 横スクロールが出ていないか
- リンク切れ（`_site` 内を走査するスクリプトを都度書いている）

過去に、エスケープ処理のミスで `script.js` が構文エラーになり全53要素が非表示のまま公開されかけた。**シェルのヒアドキュメントでJSを書くと `\r\n` が実際の改行になる事故が起きる。** Writeツールを使うこと。

---

## 6. レスポンシブ

切り替え点に依存しない設計にしてある。余白・文字・ロゴ・ナビ間隔は `clamp()` で幅に連動。切り替え点は構造変更が必要な3箇所のみ。

| 幅 | 変わること |
|---|---|
| 880px未満 | ナビをロゴ下に左揃え、ヘッダー固定を解除 |
| 760px未満 | 表を項目名と内容の縦積みに |
| 600px未満 | 相談の流れの連番と本文を縦積みに |

320/360/390/700/1024/1600px で日英とも実測済み。タップ領域は41〜44px確保。

---

## 7. 現在の未完了事項

### システム待ち

**HTTPS証明書が未発行。** DNSはGitHubのヘルスチェックで問題なしと判定されている（`is_pointed_to_github_pages_ip: true`, `is_https_eligible: true`, `caa_error: null`）。ドメイン設定を再適用して発行を促した状態。**発行されたら `https_enforced` を有効にすること。**

```bash
gh api repos/ishizukatakuya1-hub/blueaegis-site/pages   # 証明書状態の確認
gh api -X PUT repos/ishizukatakuya1-hub/blueaegis-site/pages -F https_enforced=true
```

`gh` は `C:\Program Files\GitHub CLI\gh.exe`。認証済み（`ishizukatakuya1-hub`）。

### ユーザー作業待ち

1. GitHubトークンをCowork側に受け渡し、テストPRで通し確認（期限2026-11-20）
2. 契約書・取締役決定書の押印（3点）
3. 名刺の入稿（電話番号を載せるかの判断が先）
4. Search Consoleの所有権確認 — TXTレコードは反映済み。サイトマップは送信済みとのこと

### 私（Claude）側の残作業

- **犯収法記事の英語版**（未着手・ユーザー承認済み）
- **記事のOGP画像の自動生成**（未着手・ユーザー承認済み）
- **§8のSEO自動化**（未着手・今回の依頼）

---

## 8. ★今回の依頼：SEO自動化の仕組み

> 「このHPには最強のSEO対策を自動で回す仕組みを備えて」

要件の詳細はユーザーから未聴取。**着手前に方向性を確認すること。** 以下は前セッションの見立て。

### 現状すでにあるもの

- `sitemap.xml` はビルド時に自動生成（記事追加で自動反映。手作業不要）
- `robots.txt` 設置済み
- 全ページに `canonical` / `og:*` / `description`
- 日英ページは `hreflang` で相互宣言
- Cloudflare Web Analytics 稼働中（Cookie不使用）
- Google Search Console 登録済み

### 自動化の余地がありそうなもの

1. **構造化データ（JSON-LD）の自動付与** — `Organization` / `Article` / `BreadcrumbList`。ビルド時に frontmatter から生成できる。検索結果での表示に効く
2. **OGP画像の自動生成** — 承認済みの残作業と同じ
3. **内部リンクの自動提案** — 記事間でタグや語の重なりを見て関連記事を出す
4. **メタ情報の機械検査** — title/description の長さ、重複、H1の個数、alt属性の欠落などをCIで落とす
5. **サイトマップのSearch Console自動送信** — 更新のたびにpingする
6. **表示速度の計測** — Lighthouse CIをActionsに載せる

### 注意すべき点

**「最強のSEO」を謳う施策の多くは、この会社では逆効果になる。** キーワード詰め込み、大量の薄い記事、相互リンクなどは、ブランドガイドラインの「装飾より構造」「誇張より証明」と正面から衝突する。ライセンス交渉の相手（企業の法務・知財部門）が読むサイトで、検索対策の痕跡が見えることは信用を損なう。

前セッションでの診断は「**入口の数と、到着後の転換**が効く」というもの。実際にそれで規制解説4本と選択式の問い合わせフォームを作った。自動化するなら、この延長で考えるのが筋が通る。

**着手前に、どこまでを自動化したいのか、機械的な最適化と内容の質のどちらに寄せるのかを、ボタンで確認すること。**

---

## 9. ユーザーとのやり取りで守ること

- **選択肢は必ず `AskUserQuestion`（ボタンUI）で示す。** 本文に箇条書きで並べて「どれにしますか」と聞くのは禁止（ユーザーのグローバル指示）。過去に2回指摘されている
- 応答は日本語
- ユーザーがボタンを閉じた（dismiss）場合はそこで止まり、次の指示を待つ
- 契約書の日付は西暦（JPO提出書類の【提出日】のみ元号）

---

## 10. 参照すべき記録

| 内容 | 場所 |
|---|---|
| 会社の全体像・特許ポートフォリオ | `C:\Users\user\.claude\projects\C--Users-user-Desktop-Clodecodeapp\memory\MEMORY.md` |
| 統括顧問契約・名刺・メール設定 | 同上 `project_blueaegis_mandate_advisor_2026_08_21.md` |
| ブランドガイドライン | `法人設立_BlueAegis/02_企業イメージ_ブランドガイドライン.md` |
| 特許ポートフォリオ正典 | `模擬会社/知財部/特許/CLAUDE.md` |
| 記事連携の契約 | `blueaegis-site/PUBLISHING.md` |
