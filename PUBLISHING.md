# PUBLISHING.md — 記事の自動掲載 連携仕様

Blue Aegis株式会社サイト（https://blueaegis.co.jp/）へ記事を自動掲載するための仕様です。
**記事を生成する側は、毎回このファイルを読んでから生成してください。** ここが両者の契約です。

最終更新: 2026年8月22日

---

## 1. リポジトリ

| 項目 | 値 |
|---|---|
| リポジトリ | https://github.com/ishizukatakuya1-hub/blueaegis-site |
| デフォルトブランチ | `main` |
| 公開URL | https://blueaegis.co.jp/ |
| ホスティング | GitHub Pages（GitHub Actions によるビルド配信） |
| ビルドコマンド | `node tools/build.js` |
| 依存パッケージ | なし（Node.js 20 の標準ライブラリのみ） |
| push からデプロイ完了まで | 通常 1〜3分 |

---

## 2. 記事の置き場所とファイル名

```
content/blog/YYYY-MM-DD-<slug>.md
```

- `YYYY-MM-DD` は公開日（JST）。**frontmatter の `date` と一致していること。** 不一致はビルドエラーになります。
- `<slug>` は **英小文字・数字・ハイフンのみ**。日本語・大文字・アンダースコアは使えません。
- 1つのPRにつき記事1ファイル。

例: `content/blog/2026-08-20-ai-hakusho-2026.md`

**生成されるURL**: `https://blueaegis.co.jp/blog/<slug>.html`（日付はURLに含まれません）

---

## 3. frontmatter

引継ぎ書§4のスキーマをそのまま採用しています。**変更点はありません。**

```yaml
---
title: "記事タイトル（全角40字以内目安）"
date: 2026-08-20
description: "一覧・OGP用の要約。全角80字以内目安"
tags: ["AI活用", "生産性"]
author: "Blue Aegis Media"
draft: false
sources:
  - type: primary
    publisher: "株式会社角川アスキー総合研究所（PR TIMES）"
    title: "8月20日発売『AI白書2026』独自調査…"
    published: 2026-08-10
    url: "https://prtimes.jp/main/html/rd/p/000000264.000017610.html"
  - type: secondary
    publisher: "KADOKAWAグループ ポータルサイト"
    title: "同プレスリリースの転載"
    published: 2026-08-10
    url: "https://group.kadokawa.co.jp/information/promotional_topics/article-15444.html"
---
```

### 各項目

| 項目 | 必須 | 内容 |
|---|---|---|
| `title` | ○ | 記事タイトル。60字を超えると警告 |
| `date` | ○ | 公開日。ファイル名の日付と一致必須 |
| `description` | ○ | 一覧とOGPに使う要約。120字を超えると警告 |
| `tags` | ○ | 1つ以上。空配列はエラー |
| `author` | ○ | **`Blue Aegis Media` で固定**（メディア名義） |
| `draft` | — | `true` なら公開ビルドから完全に除外。記事ページも生成されません |
| `sources` | ○ | 1件以上。**`type: primary` が最低1件必要** |

### `sources` の各要素

`type` / `publisher` / `title` / `url` は必須。`published` は任意ですが記載を推奨します。
`type` は `primary`（一次出典）か `secondary`（経由記事）のいずれか。`url` は `http://` または `https://` で始まること。

出典はビルド時に自動で記事末尾へ「一次出典」「経由記事」に分けて出力されます。
**本文中に「参考・出典」セクションを書く必要はありません。** 書くと二重になります。

---

## 4. 本文で使える記法

引継ぎ書§4の範囲に合わせ、以下のみ対応しています。**それ以外の記法は使わないでください。**

| 記法 | 書き方 |
|---|---|
| 見出し | `## ` から。`### ` も可。**`# ` は使用不可**（`title` から自動生成されます） |
| 段落 | 空行で区切る |
| 箇条書き | `- ` または `* ` |
| 番号リスト | `1. ` |
| 引用 | `> ` |
| リンク | `[表示文字](https://example.com/)` または URL の直書き |
| 表 | パイプ記法。ヘッダー行と区切り行が必要 |
| 強調 | `**太字**` / `*斜体*` / `` `コード` `` |

画像は現時点で未対応です。必要になった時点で仕様を追加します。

### 文字数

本文（空白除く）800字未満または4,000字超で警告が出ます。エラーにはなりません。
目安は1,500〜2,500字です。

---

## 5. 内容の方針

生成側で遵守してください。検証では機械的に判定できない部分です。

- 数字や調査を引くときは **必ず一次出典まで特定**し、媒体名・タイトル・公開日・URLを記録する
- 発表元の表記を正確に（例：「KADOKAWAの調査」ではなく「角川アスキー総合研究所の調査」）
- 原文のコピペ・翻訳転載はしない。事実と数字のみ抽出し、自分の言葉で再構成する。原文表現は短い引用＋引用符＋出典に限る
- 誇張した収益保証・煽り表現は使わない。数字と実例で語る

**この方針は会社のブランドガイドラインに由来します。**「証明できる範囲を超えたことは述べない」という原則を、自社の情報発信にも適用しているためです。記事の信頼性がライセンス事業の信用に直結します。

---

## 6. ブランチとPR

| 項目 | 規約 |
|---|---|
| ブランチ名 | `auto/daily-post-YYYY-MM-DD` |
| 分岐元 | `main` |
| コミット内容 | 記事1ファイルのみ |
| PRタイトル | `記事: <記事タイトル> (YYYY-MM-DD)` |
| マージ | 当面は人手で確認してからマージ |

PRを作成すると **「記事の検証」ワークフローが自動で走ります。** 検証内容は次のとおりです。

- ファイル名の規約
- frontmatter の必須項目
- `date` とファイル名の日付の一致
- `sources` に一次出典が1件以上あること
- 各 `source` の必須項目とURL形式
- 本文に `# `（H1）を使っていないこと
- ビルドが成功すること

**検証が落ちたPRはマージしないでください。** エラー内容はワークフローのログに日本語で出ます。

`main` へマージされると「サイトの配信」ワークフローが走り、1〜3分で公開されます。

---

## 7. 掲載前のローカル確認

```bash
node tools/build.js --validate-only   # 検証のみ
node tools/build.js                   # _site/ を生成
```

---

## 8. サイト内での位置づけ

サイトには性格の異なる2つの記事系統があります。**混ぜないでください。**

| 系統 | パス | 内容 | 担当 |
|---|---|---|---|
| ブログ | `/blog/` | AI活用と生産性。日次 | 自動掲載（本仕様） |
| 規制解説 | `/insights/` | 法令・規制の実務解説 | 手作業 |

規制解説は知財ライセンスの交渉相手に向けたもので、一次資料に当たって書いています。
日次のブログと同じ導線に混ぜると専門性が埋もれるため、分けています。
**自動掲載の対象は `/blog/` のみです。** `/insights/` には書き込まないでください。

---

## 9. 認証

push には Fine-grained Personal Access Token が必要です。

- 対象: このリポジトリのみ
- 権限: Contents = Read and write / Pull requests = Read and write
- 有効期限: 90日程度でローテーション

**トークンをリポジトリにコミットしないでください。** 生成側の環境変数またはタスク設定にのみ保持します。
