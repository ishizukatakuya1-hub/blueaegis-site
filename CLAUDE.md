# CLAUDE.md — Blue Aegis コーポレートサイト

このファイルはセッション開始時に自動で読まれる。**詳細の正本は `HANDOVER.md`**、記事の仕様は
`PUBLISHING.md`。ここには「読まずに触ると取り返しがつかないこと」だけを置く。

## このリポジトリ

| 項目 | 値 |
|---|---|
| 公開URL | https://blueaegis.co.jp/ |
| ビルド | `node tools/build.js` → `_site/`（検査込み。落ちたら配信されない） |
| 検証のみ | `node tools/build.js --validate-only`（frontmatter と出典） |
| 依存 | **なし。Node.js 20 の標準ライブラリだけで動かす。パッケージを増やさない** |
| 記事の置き場 | `content/blog/`（日本語・自動掲載）／`content/blog-en/`（英語・手作業） |

**このリポジトリは公開されている。** ここに書いたものは誰でも読める。
公開できない情報（出願と記事の対応、未出願案件の分野、同一商号の別法人、ローカル環境）は
`HANDOVER.private.md` にあり、**リポジトリには入らない**。手元の作業フォルダ直下に置く。
このファイルにも `HANDOVER.md` にも書き戻さないこと。

## ★書いてはいけないこと

`HANDOVER.md` §4 が正本。破ると戻せないものだけ再掲する。

1. **解決の仕組みを書かない。** この会社は多数の特許出願を保有し、その多くが国内優先権・分割・
   PCT の選択肢を残したまま係属中。自社が公開した内容は、将来の追加出願に対する先行技術になる。
   記事は**規制の内容と事業者が直面する課題まで**。締めは「運用でしのぐか、仕組みで担保するか」
   という一般論に統一している。どの記事がどの出願に対応するかは `HANDOVER.private.md`。
2. **未出願案件の分野に触れない。** 該当する4分野の名前は `HANDOVER.private.md` にある。
   記事にも技術領域にも書かない。**このファイルにも `HANDOVER.md` にも分野名を書き足さない。**
   `HANDOVER.private.md` が手元に無い環境では、判断がつかない技術分野を新たに書かないこと。
3. **一次資料で裏を取る。** 法令の条番号・施行日は社内メモやベンダー解説を根拠にしない。
   確認できなかったことは書かない（ブランドガイドライン「証明できる範囲を超えたことは述べない」）。
4. **トークン・パスワードを受け取らない・転送しない・書かない。** 例外は Cloudflare の解析トークン
   （公開ページに埋め込む前提の値）のみ。
5. **代表者名・所在地は載せない。** `tools/lib/seo.js` の `organization()` に `address` や `founder`
   を足さない。法人番号だけは 2026-08-28 のユーザー判断で解禁済み（同一商号の別法人と区別する
   ため）。
6. **相談ナビ（`index.html` の `#botflow`）を生成AI化しない。** 1 と 3 の理由でルールベースにして
   ある。2026-08-22 のユーザー判断。

## 触るときの決まり

- **`style.css` でフェードイン対象にしたセレクタは `script.js` の `SEL` にも入れる。**
  片方だけ変えると要素が非表示のまま公開される（過去に全53要素で発生）。ビルドの検査が止める。
- **全ページのフッターから privacy / disclaimer を引く。** 手書きHTMLと `tools/build.js` の `page()`
  の2経路にあるので両方直す。抜けると検査が落ちる。
- **相談ナビの `set` の値は、フォームのラジオの `value` と一字一句同じにする。** 検査が止める。
- **日付に `toISOString()` を使わない**（UTCで前日になる）。`seo.todayJst()` を使う。
- `repeat(auto-fit,minmax(300px,1fr))` は書かない。**`minmax(min(300px,100%),1fr)`** と書く。
- SVGに高さだけ指定しない。`max-width:100%` を併記する。
- **シェルのヒアドキュメントで JavaScript を書かない。** `\r\n` が実際の改行になり、構文エラーの
  まま公開されかけた事故がある。Write ツールを使う。
- 記事は1つのPRにつき1ファイル。ファイル名の日付と frontmatter の `date` を一致させる。

## 変更したら

`node tools/build.js` を通す。canonical・title/description の重複・h1・alt・構造化データ・
og:image の実在・リンク切れ・言語版の相互参照・フェードイン対象のずれ・相談ナビの値ずれを見る。
ここが通らないものは配信されない。
